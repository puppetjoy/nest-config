---
# One hour before most other restore jobs
backup_hour: '2-23/3'
restore_hour: '*/3'

# A full registry sync routinely exceeds the shared two-hour stall guard.
backup_job_active_deadline_seconds: 21600

# Ensure 'test' jobs use prod images
registry: 'registry.eyrie'
