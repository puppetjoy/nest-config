---
include:
  - 'nest::service::firefox'

plan_base: 'nest::eyrie::ai::firefox'
service_name: firefox
operator_fqdn: browser.eyrie

cert_issuer: eyrie-ca
cert_issuer_kind: ClusterIssuer
ingress_class: contour-compute
node_role: compute
image: registry.gitlab.joyfullee.me/nest/tools/firefox:latest
image_revision: '234428c2'
launch_url: about:blank
app_args: ''
vnc_options: '-disableBasicAuth'
storage_class: owl-crypt
profile_storage_size: 12Gi
ui_hard_tab_cap: 12
ui_lease_seconds: 7200
bitwarden_extension_id: '{446900e4-71c2-419f-a6a7-df9c091e268b}'
bitwarden_extension_install_url: file:///opt/nest/firefox/extensions/bitwarden.xpi

.affinity: &affinity
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
          - key: "node-role.kubernetes.io/%{lookup('node_role')}"
            operator: Exists

.tolerations: &tolerations
  - key: "node-role.kubernetes.io/%{lookup('node_role')}"
    operator: Exists
    effect: NoSchedule

.firefox_policy: &firefox_policy |-
  {
    "policies": {
      "DisableTelemetry": true,
      "DisableFirefoxAccounts": true,
      "DontCheckDefaultBrowser": true,
      "ExtensionSettings": {
        "%{lookup('bitwarden_extension_id')}": {
          "installation_mode": "force_installed",
          "install_url": "%{lookup('bitwarden_extension_install_url')}"
        }
      }
    }
  }

resources:
  backup: {}
  restore: {}
  registry_auths: {}

  certs:
    apiVersion: cert-manager.io/v1
    kind: Certificate
    metadata:
      name: "%{nest::kubernetes::service}-certs"
      namespace: "%{nest::kubernetes::namespace}"
    spec:
      secretName: "%{nest::kubernetes::service}-certs"
      issuerRef:
        name: "%{lookup('cert_issuer')}"
        kind: "%{lookup('cert_issuer_kind')}"
      dnsNames:
        - "%{lookup('operator_fqdn')}"

  browser-policy:
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: "%{nest::kubernetes::service}-browser-policy"
      namespace: "%{nest::kubernetes::namespace}"
    data:
      README.txt: |-
        Firefox/KasmVNC policy for the persistent browser.eyrie secure-browser
        canary. The public Bitwarden Firefox extension is force-installed via
        AMO using Firefox enterprise policy; this ConfigMap does not contain
        Joy vault state, browser profile state, credentials, payment/address
        details, cookies, storage, tokens, or final-purchase authority.
      policies.json: *firefox_policy

  reset-procedure:
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: "%{nest::kubernetes::service}-reset-procedure"
      namespace: "%{nest::kubernetes::namespace}"
    data:
      README.txt: |-
        Reset/revoke procedure for the browser.eyrie Firefox/KasmVNC profile:
        1. Stop the live session: kubectl -n %{nest::kubernetes::namespace} scale deployment/%{nest::kubernetes::service} --replicas=0
        2. Wipe persistent Firefox/KasmVNC user state: kubectl -n %{nest::kubernetes::namespace} delete pvc %{nest::kubernetes::service}-profile
        3. Recreate managed resources: bolt plan run nest::eyrie::ai::deploy_firefox
        4. Joy should re-enter any required login and Bitwarden state directly in the visible browser UI.

        This canary exposes only the Nest Firefox/KasmVNC operator UI at
        https://%{lookup('operator_fqdn')}/. Hermes invokes the in-container
        Firefox UI bridge through Kubernetes exec; there is no Remote
        Debugging, WebDriver, Marionette, BiDi, CDP, or hidden browser-context
        endpoint. The bridge uses only X11 input, AT-SPI readback, and visible
        X11 screenshots. Raw VNC credentials, cookies, storage, profile files,
        request headers, vault data, and payment/address secrets are never
        exposed through the bridge.

  profile-storage:
    apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: "%{nest::kubernetes::service}-profile"
      namespace: "%{nest::kubernetes::namespace}"
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: "%{lookup('profile_storage_size')}"
      storageClassName: "%{lookup('storage_class')}"

  metadata-egress-guard:
    apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: "%{nest::kubernetes::service}-metadata-egress-guard"
      namespace: "%{nest::kubernetes::namespace}"
    spec:
      podSelector:
        matchLabels:
          app: "%{nest::kubernetes::service}"
      policyTypes:
        - Egress
      egress:
        - to:
            - ipBlock:
                cidr: '0.0.0.0/0'
                except:
                  - '169.254.0.0/16'
        - to:
            - ipBlock:
                cidr: '::/0'
                except:
                  - 'fe80::/10'

  deployment:
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: "%{nest::kubernetes::service}"
      namespace: "%{nest::kubernetes::namespace}"
      labels:
        app: "%{nest::kubernetes::service}"
        browser.joyfullee.me/app: firefox
        browser.joyfullee.me/image-family: nest-tools-firefox
    spec:
      replicas: 1
      strategy:
        type: Recreate
      selector:
        matchLabels:
          app: "%{nest::kubernetes::service}"
      template:
        metadata:
          labels:
            app: "%{nest::kubernetes::service}"
            browser.joyfullee.me/app: firefox
            browser.joyfullee.me/image-family: nest-tools-firefox
        spec:
          affinity: *affinity
          tolerations: *tolerations
          securityContext:
            fsGroup: 1000
          containers:
            - name: kasm-firefox
              image: "%{lookup('image')}"
              imagePullPolicy: Always
              securityContext:
                capabilities:
                  add:
                    # Firefox's content sandbox needs this to create a root
                    # user namespace inside the container. Without it,
                    # Firefox requires MOZ_DISABLE_*_SANDBOX and displays a
                    # persistent "security sandbox is disabled" warning.
                    - SETFCAP
              env:
                - name: VNC_PW
                  value: unused
                - name: VNCOPTIONS
                  value: "%{lookup('vnc_options')}"
                - name: NEST_FIREFOX_IMAGE_REVISION
                  value: "%{lookup('image_revision')}"
                - name: LAUNCH_URL
                  value: "%{lookup('launch_url')}"
                - name: KASM_RESTRICTED_FILE_CHOOSER
                  value: '1'
                - name: FIREFOX_WIDTH
                  value: '1365'
                - name: FIREFOX_HEIGHT
                  value: '768'
                - name: APP_ARGS
                  value: "%{lookup('app_args')}"
                - name: FIREFOX_UI_HARD_TAB_CAP
                  value: "%{lookup('ui_hard_tab_cap')}"
                - name: FIREFOX_UI_LEASE_SECONDS
                  value: "%{lookup('ui_lease_seconds')}"
              ports:
                - name: https
                  containerPort: 6901
              readinessProbe:
                exec:
                  command:
                    - /bin/sh
                    - -ec
                    - >-
                      fmt='%'; fmt="${fmt}{http_code}";
                      code=$(curl -ks -o /dev/null -w "${fmt}" https://127.0.0.1:6901/ || true);
                      test "${code}" -ge 200 -a "${code}" -lt 400
                periodSeconds: 5
                timeoutSeconds: 2
                failureThreshold: 24
              livenessProbe:
                tcpSocket:
                  port: https
                periodSeconds: 20
                timeoutSeconds: 2
              volumeMounts:
                - name: profile
                  mountPath: /home/kasm-user
                - name: browser-policy
                  mountPath: /usr/lib64/firefox/distribution/policies.json
                  subPath: policies.json
                  readOnly: true
                - name: dshm
                  mountPath: /dev/shm

          volumes:
            - name: profile
              persistentVolumeClaim:
                claimName: "%{nest::kubernetes::service}-profile"
            - name: browser-policy
              configMap:
                name: "%{nest::kubernetes::service}-browser-policy"
            - name: dshm
              emptyDir:
                medium: Memory
                sizeLimit: 1Gi

  service:
    apiVersion: v1
    kind: Service
    metadata:
      name: "%{nest::kubernetes::service}"
      namespace: "%{nest::kubernetes::namespace}"
      annotations:
        projectcontour.io/upstream-protocol.tls: '6901'
    spec:
      type: ClusterIP
      selector:
        app: "%{nest::kubernetes::service}"
      ports:
        - name: https
          port: 6901
          targetPort: https

  ingress:
    apiVersion: projectcontour.io/v1
    kind: HTTPProxy
    metadata:
      name: "%{nest::kubernetes::service}"
      namespace: "%{nest::kubernetes::namespace}"
    spec:
      ingressClassName: "%{lookup('ingress_class')}"
      virtualhost:
        fqdn: "%{lookup('operator_fqdn')}"
        tls:
          secretName: "%{nest::kubernetes::service}-certs"
      routes:
        - conditions:
            - prefix: /
          enableWebsockets: true
          services:
            - name: "%{nest::kubernetes::service}"
              port: 6901

values: {}
