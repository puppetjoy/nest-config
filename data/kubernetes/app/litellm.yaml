---
resources:
  registry_auths: {}
  backup: {}
  restore: {}

values:
  affinity: "%{alias('affinity')}"
  tolerations: "%{alias('tolerations')}"
  db:
    deployStandalone: false
  migrationJob:
    enabled: false
  proxy_config:
    general_settings:
      master_key: null
  service:
    port: 80
