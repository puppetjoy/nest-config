---
description: 'Deploy an OpenVox-backed Puppet service'

parameters:
  service:
    description: 'Puppet service to deploy'
    type: String
    default: 'puppet'

  namespace:
    description: 'Kubernetes namespace'
    type: String
    default: 'default'

  deploy:
    description: 'Run or skip the deployment'
    type: Boolean
    default: true

  init:
    description: 'Deploy initial revision'
    type: Boolean
    default: false

  render_to:
    description: 'Render the template to a file'
    type: String
    default: ''

steps:
  - description: 'Deploy OpenVox'
    plan: nest::kubernetes::deploy
    parameters:
      namespace: $namespace
      service: $service
      app: 'openvox'
      chart: 'openvox/puppetserver'
      repo_url: 'https://openvoxproject.github.io/openvox-helm-chart'
      version: '10.1.1'
      deploy: $deploy
      init: $init
      wait: $init
      render_to: $render_to

  - description: 'Restore initial OpenVox resources'
    plan: nest::eyrie::openvox::restore
    parameters:
      namespace: $namespace
      service: $service
      service_name: $service
      restore: $deploy and $init and $render_to == ''
