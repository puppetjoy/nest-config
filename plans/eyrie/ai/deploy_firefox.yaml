---
description: 'Deploy Firefox/Kasm secure browser service'

parameters:
  deploy:
    description: 'Run or skip the deployment'
    type: Boolean
    default: true

  render_to:
    description: 'Just render the template'
    type: String
    default: ''

steps:
  - description: 'Deploy Firefox/Kasm secure browser service'
    plan: 'nest::kubernetes::deploy'
    parameters:
      service: 'firefox'
      app: 'firefox'
      namespace: 'ai'
      init: false
      deploy: $deploy
      render_to: $render_to
