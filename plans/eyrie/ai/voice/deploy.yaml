---
description: 'Deploy a voice speech API instance'

parameters:
  service:
    description: 'voice service to deploy'
    type: String

  namespace:
    description: 'Kubernetes namespace'
    type: String
    default: 'ai'

  deploy:
    description: 'Run or skip the deployment'
    type: Boolean
    default: true

  init:
    description: 'Deploy initial revision'
    type: Boolean
    default: false

  render_to:
    description: 'Just render the template'
    type: String
    default: ''

steps:
  - description: 'Deploy the voice instance'
    plan: nest::kubernetes::deploy
    parameters:
      namespace: $namespace
      service: $service
      app: 'speaches'
      deploy: $deploy
      init: $init
      wait: $init
      render_to: $render_to
