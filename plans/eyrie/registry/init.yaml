---
description: 'Initialize a registry instance'

parameters:
  namespace:
    description: 'Kubernetes namespace'
    type: String
    default: 'default'

  service:
    description: 'Registry service to restore'
    type: String
    default: 'registry'

  service_name:
    description: 'Kubernetes workload service name'
    type: String
    default: $service

  # This plan may be called during deployment
  init:
    description: 'Run or skip initialization'
    type: Boolean
    default: true

steps:
  - description: "Restore registry instance"
    plan: nest::eyrie::registry::restore
    parameters:
      namespace: $namespace
      service: $service
      service_name: $service_name
      restore: $init
