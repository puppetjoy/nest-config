<%- | String[1] $venv_python | -%>
#!/bin/sh
set -eu
exec <%= $venv_python %> -m hermes_cli.main --profile "$1" dashboard --host "${HERMES_DASHBOARD_BIND_HOST}" --port "${HERMES_DASHBOARD_PORT}" --no-open --skip-build --insecure --isolated
