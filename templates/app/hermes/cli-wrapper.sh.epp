<%- |
  String[1] $venv_dir,
  String[1] $ca_bundle_file,
| -%>
#!/bin/sh
set -eu
export SSL_CERT_FILE=<%= $ca_bundle_file %>
export REQUESTS_CA_BUNDLE=<%= $ca_bundle_file %>
export CURL_CA_BUNDLE=<%= $ca_bundle_file %>
export SSL_CERT_DIR=/etc/ssl/certs
exec <%= $venv_dir %>/bin/hermes "$@"
