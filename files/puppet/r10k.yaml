---
cachedir: '/var/cache/r10k'
# GitLab's SSH endpoint closes concurrent fetches from the legacy deploy host.
pool_size: 1

sources:
  nest:
    remote: 'git@gitlab.joyfullee.me:nest/config.git'
    basedir: '/etc/puppetlabs/code/environments'
    ignore_branch_prefixes:
      - 'talon/'
