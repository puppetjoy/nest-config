diff -uNr a/ui-tui/src/components/branding.tsx b/ui-tui/src/components/branding.tsx
--- a/ui-tui/src/components/branding.tsx	2026-06-04 03:10:33.195775644 -0400
+++ b/ui-tui/src/components/branding.tsx	2026-06-04 03:10:33.209488793 -0400
@@ -32,12 +32,18 @@
   return (
     <Box flexDirection="column" height={lines.length} width={artWidth(lines)}>
       {lines.map((spans, i) => (
-        <Text key={i} wrap="truncate-end">
-          {spans.map(([fg, bg, text], j) => (
-            <Text backgroundColor={bg || undefined} color={fg || undefined} key={j}>
-              {text}
-            </Text>
-          ))}
+        <ArtRow key={i} spans={spans} />
+      ))}
+    </Box>
+  )
+}
+
+function ArtRow({ spans }: { spans: [string, string, string][] }) {
+  return (
+    <Box flexDirection="row" height={1} width={spans.reduce((n, [, , text]) => n + text.length, 0)}>
+      {spans.map(([fg, bg, text], j) => (
+        <Text backgroundColor={bg || undefined} color={fg || undefined} key={j} wrap="truncate-end">
+          {text}
         </Text>
       ))}
     </Box>
