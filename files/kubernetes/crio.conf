[crio.image]
# CRI-O default (10s) is too aggressive on ZFS-backed storage.
# Large blobs can exceed 10s to commit even on a healthy pool.
# 10m accommodates worst-case downloads over the internal network.
pull_progress_timeout = "10m"
