[crio.image]
# CRI-O's 10s default is too aggressive for ZFS-backed storage when
# committing large blobs. This is a no-progress timeout, not a total
# download timeout, so active pulls may continue beyond one minute.
pull_progress_timeout = "1m"
