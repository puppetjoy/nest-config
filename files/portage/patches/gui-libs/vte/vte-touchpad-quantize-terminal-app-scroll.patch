diff --git a/src/vte.cc b/src/vte.cc
index 5f2c8f82..e00d3c22 100644
--- a/src/vte.cc
+++ b/src/vte.cc
@@ -10138,26 +10138,78 @@ Terminal::widget_mouse_scroll(vte::platform::ScrollEvent const& event)
 	gdouble v;
         gint cnt_x, cnt_y, i;
 	int button;
+        /*
+         * Touchpad tuning for terminal-app scroll forwarding:
+         *
+         * GTK surface scroll events arrive as high-resolution deltas, but
+         * terminal applications only accept discrete wheel-style events.  Keep
+         * precise input on the same accumulator path as wheel input, but scale
+         * it down first and quantise it with a smaller first step so the motion
+         * feels closer to Ghostty's touchpad handling.
+         *
+         * If this patch needs to be forward-ported, the key call sites to
+         * preserve are:
+         *   - Widget::event_scroll()/scroll_event_from_gdk() tagging precise
+         *     versus discrete input
+         *   - Widget::event_scroll_begin()/event_scroll_end() resetting the
+         *     accumulator at gesture boundaries
+         *   - Terminal::widget_mouse_scroll() keeping precise input on the
+         *     existing accumulator path instead of per-row quantisation
+         *
+         * These values were tuned empirically against Ghostty on GNOME:
+         *   precise_scroll_multiplier: overall touchpad speed
+         *   first_step: motion needed for the first emitted line
+         */
+        auto const precise_scroll_multiplier = 0.07;
+        auto const quantise_precise_scroll = [](double* delta) noexcept {
+                auto const sign = *delta < 0 ? -1 : 1;
+                auto const magnitude = fabs(*delta);
+                auto const first_step = 0.20;
+                auto count = 0;
+                auto consumed = 0.;
+
+                if (magnitude >= first_step) {
+                        count = 1 + int(magnitude - first_step);
+                        consumed = first_step + (count - 1);
+                        *delta = sign * (magnitude - consumed);
+                        return sign * count;
+                }
+
+                return 0;
+        };
+        auto scroll_dx = event.dx();
+        auto scroll_dy = event.dy();
+
+        if (event.is_precise()) {
+                scroll_dx *= precise_scroll_multiplier;
+                scroll_dy *= precise_scroll_multiplier;
+        }
 
         m_modifiers = event.modifiers();
-        m_mouse_smooth_scroll_x_delta += event.dx();
-        m_mouse_smooth_scroll_y_delta += event.dy();
+        m_mouse_smooth_scroll_x_delta += scroll_dx;
+        m_mouse_smooth_scroll_y_delta += scroll_dy;
 
         /* If we're running a mouse-aware application, map the scroll event to button presses on buttons 4-7. */
 	if (m_mouse_tracking_mode != MouseTrackingMode::eNONE) {
-                cnt_x = m_mouse_smooth_scroll_x_delta;
-                cnt_y = m_mouse_smooth_scroll_y_delta;
+                if (event.is_precise()) {
+                        cnt_x = quantise_precise_scroll(&m_mouse_smooth_scroll_x_delta);
+                        cnt_y = quantise_precise_scroll(&m_mouse_smooth_scroll_y_delta);
+                } else {
+                        cnt_x = m_mouse_smooth_scroll_x_delta;
+                        cnt_y = m_mouse_smooth_scroll_y_delta;
+                        m_mouse_smooth_scroll_x_delta -= cnt_x;
+                        m_mouse_smooth_scroll_y_delta -= cnt_y;
+                }
                 if (cnt_x == 0 && cnt_y == 0)
 			return true;
 
                 /* Need to ensure the ringview is updated. */
                 ringview_update();
 
-                m_mouse_smooth_scroll_x_delta -= cnt_x;
-                m_mouse_smooth_scroll_y_delta -= cnt_y;
 		_vte_debug_print(VTE_DEBUG_EVENTS,
-                                 "Scroll application by %d lines, %d columns, smooth scroll delta set back to y=%f, x=%f\n",
-                                 cnt_y, cnt_x, m_mouse_smooth_scroll_y_delta, m_mouse_smooth_scroll_x_delta);
+                                 "Scroll application by %d lines, %d columns, precise=%d, multiplier=%f, smooth scroll delta set back to y=%f, x=%f\n",
+                                 cnt_y, cnt_x, event.is_precise(), precise_scroll_multiplier,
+                                 m_mouse_smooth_scroll_y_delta, m_mouse_smooth_scroll_x_delta);
 
                 button = cnt_y > 0 ? 5 : 4;
                 if (cnt_y < 0)
@@ -10195,13 +10247,18 @@ Terminal::widget_mouse_scroll(vte::platform::ScrollEvent const& event)
 		char *normal;
 		gsize normal_length;
 
-                cnt_y = v * m_mouse_smooth_scroll_y_delta;
+                if (event.is_precise()) {
+                        cnt_y = quantise_precise_scroll(&m_mouse_smooth_scroll_y_delta);
+                } else {
+                        cnt_y = v * m_mouse_smooth_scroll_y_delta;
+                        if (cnt_y != 0)
+                                m_mouse_smooth_scroll_y_delta -= cnt_y / v;
+                }
                 if (cnt_y == 0)
 			return true;
-                m_mouse_smooth_scroll_y_delta -= cnt_y / v;
 		_vte_debug_print(VTE_DEBUG_EVENTS,
-                                "Scroll by %d lines, smooth scroll y_delta set back to %f\n",
-                                cnt_y, m_mouse_smooth_scroll_y_delta);
+                                "Scroll by %d lines, precise=%d, multiplier=%f, smooth scroll y_delta set back to %f\n",
+                                cnt_y, event.is_precise(), precise_scroll_multiplier, m_mouse_smooth_scroll_y_delta);
 
                 /* In the alternate screen there is no scrolling, so fake a few cursor keystrokes (only vertically). */
 		_vte_keymap_map (
@@ -10228,6 +10285,13 @@ Terminal::widget_mouse_scroll(vte::platform::ScrollEvent const& event)
         return false;
 }
 
+void
+Terminal::reset_mouse_smooth_scroll() noexcept
+{
+        m_mouse_smooth_scroll_x_delta = 0.;
+        m_mouse_smooth_scroll_y_delta = 0.;
+}
+
 bool
 Terminal::set_audible_bell(bool setting)
 {
diff --git a/src/vteinternal.hh b/src/vteinternal.hh
index 4d26a4f9..5ca77ee4 100644
--- a/src/vteinternal.hh
+++ b/src/vteinternal.hh
@@ -1108,6 +1108,7 @@ public:
         void widget_mouse_enter(vte::platform::MouseEvent const& event);
         void widget_mouse_leave(vte::platform::MouseEvent const& event);
         bool widget_mouse_scroll(vte::platform::ScrollEvent const& event);
+        void reset_mouse_smooth_scroll() noexcept;
 #if VTE_GTK == 4
         bool widget_key_modifiers(unsigned modifiers);
 #endif /* VTE_GTK == 4 */
diff --git a/src/widget.cc b/src/widget.cc
index 0daaed1e..906295ef 100644
--- a/src/widget.cc
+++ b/src/widget.cc
@@ -1245,7 +1245,9 @@ Widget::event_scroll_begin(GtkEventControllerScroll* controller)
 {
         _vte_debug_print(VTE_DEBUG_EVENTS, "Scroll begin\n");
 
-        // FIXMEgtk4
+        /* Keep partial precise-scroll remainder from leaking across gestures. */
+        if (gtk_event_controller_scroll_get_unit(controller) == GDK_SCROLL_UNIT_SURFACE)
+                terminal()->reset_mouse_smooth_scroll();
 }
 
 bool
@@ -1260,6 +1262,9 @@ Widget::event_scroll(GtkEventControllerScroll* controller,
                 return false;
 
         return terminal()->widget_mouse_scroll({gdk_event_get_modifier_state(event),
+                                                gtk_event_controller_scroll_get_unit(controller) == GDK_SCROLL_UNIT_SURFACE
+                                                        ? ScrollEvent::Unit::eSURFACE
+                                                        : ScrollEvent::Unit::eWHEEL,
                                                 dx, dy});
 }
 
@@ -1268,7 +1273,9 @@ Widget::event_scroll_end(GtkEventControllerScroll* controller)
 {
         _vte_debug_print(VTE_DEBUG_EVENTS, "Scroll end\n");
 
-        // FIXMEgtk4
+        /* Keep partial precise-scroll remainder from leaking across gestures. */
+        if (gtk_event_controller_scroll_get_unit(controller) == GDK_SCROLL_UNIT_SURFACE)
+                terminal()->reset_mouse_smooth_scroll();
 }
 
 void
@@ -1758,6 +1765,7 @@ Widget::scroll_event_from_gdk(GdkEvent* event) const
         if (gdk_event_get_pointer_emulated(event))
                 return std::nullopt;
 
+        auto unit = ScrollEvent::Unit::eWHEEL;
         auto dx = double{}, dy = double{};
         if (!gdk_event_get_scroll_deltas(event, &dx, &dy)) {
                 auto dir = GdkScrollDirection{};
@@ -1772,10 +1780,12 @@ Widget::scroll_event_from_gdk(GdkEvent* event) const
                 case GDK_SCROLL_SMOOTH:
                 default: __builtin_unreachable();
                 }
+        } else {
+                unit = ScrollEvent::Unit::eSURFACE;
         }
 
 
-        return ScrollEvent{read_modifiers_from_gdk(event), dx, dy};
+        return ScrollEvent{read_modifiers_from_gdk(event), unit, dx, dy};
 }
 
 #endif /* VTE_GTK == 3 */
diff --git a/src/widget.hh b/src/widget.hh
index 4dd78c2c..93cc7ba5 100644
--- a/src/widget.hh
+++ b/src/widget.hh
@@ -226,13 +226,20 @@ class ScrollEvent : public EventBase {
 
 protected:
 
+        enum class Unit {
+                eWHEEL,
+                eSURFACE,
+        };
+
         ScrollEvent() noexcept = default;
 
         constexpr ScrollEvent(unsigned modifiers,
+                              Unit unit,
                               double dx,
                               double dy) noexcept
                 : EventBase{EventBase::Type::eMOUSE_SCROLL},
                   m_modifiers{modifiers},
+                  m_unit{unit},
                   m_dx{dx},
                   m_dy{dy}
         {
@@ -247,11 +254,14 @@ public:
         ScrollEvent& operator=(ScrollEvent&&) = delete;
 
         constexpr auto modifiers() const noexcept { return m_modifiers; }
+        constexpr auto unit() const noexcept { return m_unit; }
+        constexpr auto is_precise() const noexcept { return m_unit == Unit::eSURFACE; }
         constexpr auto dx()        const noexcept { return m_dx;        }
         constexpr auto dy()        const noexcept { return m_dy;        }
 
 private:
         unsigned m_modifiers;
+        Unit m_unit{Unit::eWHEEL};
         double m_dx;
         double m_dy;
 }; // class ScrollEvent
