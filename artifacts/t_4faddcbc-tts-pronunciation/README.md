# t_4faddcbc Chatterbox TTS pronunciation artifacts

This artifact set reproduces and verifies the Hermes Chatterbox speech-only pronunciation fix.

## Source text

`sample.txt` preserves the visual notification text exactly as Telegram should continue to show it:

```text
Agent request ar-20260610-143610-99551e is blocked by task t_922b6ec2. Commit 9204d2f9 updates voice-chatterbox for KubeCM, llama-qwen, ROCm, OpenVox, and https://talon.eyrie/api/events.
```

`sample-normalized.txt` is the speech-only text sent to Chatterbox by the patched wrapper:

```text
Agent request A R two zero two six zero six one zero, one four three six one zero, nine nine five five one E is blocked by task T nine two two B six E C two. Commit nine two zero four D two F nine updates voice-chatterbox for Kube C M, llama Qwen, R O C M, Open Vox, and H T T P S, talon dot eyrie slash api slash events.
```

## Audio files

Before files were generated by calling `ai/voice-chatterbox` directly with the unnormalized visual text, matching the previous Hermes provider behavior. After files were generated through the patched `files/app/hermes/chatterbox-tts-command.py` wrapper.

| File | Voice | Input path | Audio seconds | SHA-256 |
|---|---|---|---:|---|
| `before-talon.wav` | Talon | direct unnormalized API call | 24.840 | `76820a8d204f503f8093dbd9cb91a97906c93362c5fc2df68e77e8caaec0a35c` |
| `after-talon.wav` | Talon | patched Hermes wrapper | 26.870 | `36446194018e214506ada56ab06bb7771cf3245ecd658b03664061973338ed63` |
| `before-star.wav` | Star | direct unnormalized API call | 25.670 | `1606b32a3f4a3327e7d38839e066c9054a0517a3c0faa4d07e3b1395a2db160e` |
| `after-star.wav` | Star | patched Hermes wrapper | 27.030 | `f85ff14c08cce1a4698e62f1059c0f961c3e4eaf6204248c504d6a5dd558e710` |

`manifest.json` records endpoint, input text, output paths, byte sizes, wall times, audio duration/format, and hashes.

## STT proxy evidence

The local STT transcript is not the source of truth for pronunciation quality, but it gives a useful machine-checkable proxy for Joy review.

- Before Talon: `Agent request R2600-1436-9951E ... task T-I22B60T ...`
- After Talon: `Agent request AR20260610, 143610, 99551E ... task T922B6EC2 ...`
- Before Star: `Agent request R2066101436109951D ... task T6T922B6610 ...`
- After Star: `Agent request AR20260610, 143610, 99551E ... task T922B6EC2 ...`

The before clips reproduce the ID garbling; the after clips keep the full task/request digits and hex letters intelligible enough for STT to recover the canonical IDs without changing Telegram visual text.

## Production-path proof

`verify_wrapper_payload.py` starts a local stub OpenAI-compatible TTS endpoint and runs the exact Hermes command-provider wrapper. `stub-captured-payload.json` shows the payload input sent by the wrapper is normalized speech text while `sample.txt` remains canonical visual text.

`manifests/lib/hermes.pp` already configures the Chatterbox provider command as:

```text
${install_dir}/bin/hermes-chatterbox-tts --endpoint ${tts_chatterbox_endpoint} --text-file {input_path} --output {output_path} --voice {voice} --model {model} --format {format} --speed {speed}
```

so deploying the patched wrapper changes the production TTS path only. STT behavior is untouched.

## Rollback

Revert the change to `files/app/hermes/chatterbox-tts-command.py` and redeploy/apply `nest::app::hermes` so `/opt/hermes-agent/bin/hermes-chatterbox-tts` returns to pass-through text behavior. The visual Telegram notification path does not need rollback because it is not changed by this patch.
