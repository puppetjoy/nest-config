# browser.eyrie Nest Firefox parity validation — 2026-07-02

Task: `t_7a68ae00` / Agent Request `^1367`.

## Safety boundary

- No orders were placed.
- No account, payment, address, cart-setting, or Bitwarden vault content was inspected or modified.
- No cookies, local storage, raw CDP payloads, request headers, raw DOM dumps, raw order numbers, full address/payment/account details, or sensitive screenshots were captured into this worker-visible artifact.
- The latest KasmVNC live revalidation used only the public `https://example.com/` page plus source/runtime filesystem checks for Firefox/KasmVNC/font/trust presence.
- The Amazon order-history / owner-only post-purchase rows still require Joy takeover when Joy wants those sensitive baseline rows rerun from a logged-in session.

## Source and rollout summary

- Nest config `origin/main` contains the Firefox cutover, KasmVNC source-build fix, resize-grow fix, and rollout revision commits through `4a530043`.
- The image build uses Portage Firefox plus source-built KasmVNC from the pinned `kasmtech/KasmVNC` checkout, managed with `nest::lib::src_repo` and `nest::lib::build`.
- `browser.eyrie` uses `registry.gitlab.joyfullee.me/nest/tools/firefox:latest` with `imagePullPolicy: Always` and source-managed `image_revision: '9fc8b132'` to force the live rollout to the rebuilt image.
- The published registry tag verifies as image id `11d325896ccc97b691593b29152433a8204a636bc61bc57a1a3b91d1b537e266`, digest `sha256:fa5c3c13ee24a6fa2b8ed72a6173580f4a1bab46bf3641e4f3cac93415c8652e`.
- The live Firefox pod image after the resize-grow rollout is `registry.gitlab.joyfullee.me/nest/tools/firefox@sha256:06907fbcacf2b602540ff053741aebdf1b8f5fb4ce6a1a54b9fcd22158e23ee6`.
- The live Camofox deployment uses `registry.gitlab.joyfullee.me/nest/tools/camofox:latest` with `imagePullPolicy: Always`.
- Puppet code deploy succeeded to legacy/test/prod OpenVox at `4a530043`.
- KubeCM deployed Firefox Helm revision 14; `deployment/firefox` observed generation 12 is ready 1/1 on owl.

## Findings fixed during validation

1. Firefox BiDi could attach but navigation failed because Portage Firefox content processes crashed under the unavailable container user-namespace sandbox. The wrapper disables Firefox process sandboxes inside the pod while preserving the Kubernetes, NetworkPolicy, backend identity, sanitization, and final-purchase boundaries.
2. Firefox initially showed `SEC_ERROR_UNKNOWN_ISSUER` for `browser.eyrie` because `www-client/firefox` overwrote `libnssckbi.so` after the base cert class linked it to p11-kit trust. The tool class orders Firefox before `nest::base::certs`; the live image has `/usr/lib64/libnssckbi.so -> /usr/lib64/pkcs11/p11-kit-trust.so`.
3. The interim source-managed image used x11vnc plus noVNC and then client-side `resize=scale`, which made the Firefox surface appear enlarged compared with upstream KasmVNC. The current source replaces that shim with source-built KasmVNC from `kasmtech/KasmVNC`, built via `nest::lib::src_repo` and `nest::lib::build`, and serves the KasmVNC web client directly from `/opt/kasmweb/share/kasmvnc/www`.
4. The wrapper preserved runtime `HOME=/root`, putting Firefox state on ephemeral root storage instead of the mounted PVC. It now defaults `HOME` to `/home/kasm-user` unless `FIREFOX_HOME` is explicitly set; the live pod creates the profile under `/home/kasm-user/.mozilla/firefox/nest-secure-browser`.
5. Firefox could open smaller than the VNC framebuffer without a desktop window manager. The wrapper derives `FIREFOX_WIDTH`/`FIREFOX_HEIGHT` from `VNC_RESOLUTION`, passes them to Firefox, and uses `xdotool` to move/resize the visible Firefox window to `0,0 1365x768`. KasmVNC owns the browser-window/framebuffer behavior after that; there is no noVNC client-side scaling shim in the live path.
6. The Bitwarden enterprise policy was mounted at `/usr/lib/firefox/distribution/policies.json`, while Gentoo Firefox reads `/usr/lib64/firefox/distribution/policies.json`. The workload now mounts policy at the Gentoo path.
7. The image caches the public Bitwarden XPI at `/opt/nest/firefox/extensions/bitwarden.xpi` and exposes it through `/usr/lib64/firefox/distribution/extensions/{446900e4-71c2-419f-a6a7-df9c091e268b}.xpi`; the live profile has the extension XPI under `/home/kasm-user/.mozilla/firefox/nest-secure-browser/extensions/`.
8. The floating `latest` image tag did not by itself change the Deployment pod template, so Helm initially left the old pod running. `data/kubernetes/app/firefox.yaml` now carries a source-managed `image_revision` env value so KubeCM can perform a real rollout after this image rebuild.
9. KasmVNC accepted upward SetDesktopSize resize events, but the container has no full desktop window manager to grow the Firefox top-level window after startup. The wrapper now keeps a small `xdotool` loop running so Firefox follows the live KasmVNC framebuffer for both shrink and grow events, and the built KasmVNC web client forces `resize=remote` before its module starts so stale client-side `resize=scale` settings cannot reintroduce the enlargement path.

## Validation matrix

| Test name | Purpose | Method | Expected | Actual | Status | Artifacts/bindings | Safety notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Static validation | Verify source syntax/lint | `pdk validate`; YAML parse with aliases enabled; `sh -n files/firefox-browser/nest-firefox-browser.sh`; `bash -n files/firefox-browser/build-kasmvnc.sh`; `sh -n files/firefox-browser/smoke-firefox.sh`; `git diff --check` | All pass | All passed after the KasmVNC source-build and rollout-revision commits | PASS | task run output | No live browser access |
| Source push | Publish source-managed fix | `git push git@gitlab.joyfullee.me:nest/config.git HEAD:main` | `origin/main` advances to fix commit | `origin/main` advanced to `4a530043` before the evidence-doc update commit | PASS | task run output | Source-managed rollout |
| Build and publish Firefox image | Build Nest-owned Portage Firefox/KasmVNC image | `./bin/build firefox zen5 deploy=true emerge_default_opts="--jobs=4 --load-average=12" makeopts="-j8"`; final resize-grow rebuild used the same command | Build, smoke test, commit, and registry push succeed | Resize-grow rebuild completed in 10m13s, smoke-tested Firefox 140.9.0esr, and pushed image config `4fd4859e...`; live pod later pulled digest `sha256:06907fbcacf2b602540ff053741aebdf1b8f5fb4ce6a1a54b9fcd22158e23ee6` | PASS | task run output | No browser secrets in image |
| Puppet code deploy | Deploy source to legacy/test/prod Puppet/OpenVox | `./bin/bolt-wrapper plan run nest::puppet::deploy --stream` | Code servers converge to latest commit | legacy/test/prod verified `4a530043` | PASS | task run output | Inventory-backed Bolt path |
| KubeCM Firefox deploy | Deploy browser.eyrie app from source | `./bin/bolt-wrapper plan run nest::eyrie::ai::deploy_firefox --stream`; `kubectl -n ai rollout status deployment/firefox` | Helm upgrade succeeds and pod ready | Helm revision 14 deployed; generation 12 observed; deployment ready 1/1; revision env `9fc8b132` present | PASS | task run output | Source-managed KubeCM path |
| Deployment identity | Prove secure browser binds to Nest Firefox workload | `secure_browser_status`; `kubectl -n ai get deploy/firefox` | `deployment/firefox`, `nest/tools/firefox`, backend identity ok | `backend_identity.ok=true`, expected image regex `nest/tools/firefox`, workload `deployment/firefox`, ready replicas 1/1 | PASS | secure_browser_status result | No raw CDP URL returned |
| Camofox font-bearing image | Verify Camofox remains on Nest image with font support | `kubectl -n ai get deploy/camofox`; prior build/deploy evidence in task run | Camofox on Nest image and ready | `deployment/camofox` ready 1/1, image `registry.gitlab.joyfullee.me/nest/tools/camofox:latest`, pull policy Always | PASS | task run output | No browser secrets |
| Firefox fonts and trust | Verify Nest fonts and Eyrie trust in image | `fc-match "Noto Color Emoji"`; `ls -l /usr/lib64/libnssckbi.so`; `curl -ksSL https://browser.eyrie/` | Emoji/font available, NSS trust linked to p11-kit, browser.eyrie certificate trusted | Noto Color Emoji resolved; `libnssckbi.so` symlinked to p11-kit; browser.eyrie returned `200`; Firefox rendered public pages through the live secure-browser bridge | PASS | task run output | No sensitive pages |
| Browser.eyrie KasmVNC autoconnect | Verify Joy-visible operator URL auto-enters KasmVNC | `browser_navigate('https://browser.eyrie/')`; accessibility snapshot; safe visual inspection | KasmVNC web client connects without a manual Connect click | Browser snapshot showed KasmVNC controls plus `Disconnect Connected (encrypted) to x11`; visual check showed Firefox already connected and rendering Example Domain | PASS | `/home/joy/.hermes/profiles/talon/browser_screenshots/browser_screenshot_e41a266a.png` | Public example.com content only during visual check |
| Browser.eyrie KasmVNC fill/resize | Verify KasmVNC canvas and Firefox window match upstream behavior without noVNC `resize=scale` enlargement | Safe visual check; browser viewport metrics; in-pod process/geometry checks | Canvas and Firefox window match viewport without client-side scaling enlargement | Screenshot dimensions were 1365x768; KasmVNC served `Xvnc`; Firefox launched at `--width 1365 --height 768`; KasmVNC owns the web client/framebuffer path | PASS | `/home/joy/.hermes/profiles/talon/browser_screenshots/browser_screenshot_e41a266a.png` | Public example.com page only; no vault/account content |
| Browser.eyrie KasmVNC shrink/grow resize | Reproduce Joy's grow-after-shrink failure and verify Firefox grows with the KasmVNC framebuffer | Headless Chrome CDP attached to `https://browser.eyrie/`; set viewport to 1200x613, then 1000x650, then 1700x950; after each resize, checked `localStorage.resize`, KasmVNC connection state, `xdotool getdisplaygeometry`, and Firefox window geometry in the live pod | KasmVNC client stays in `resize=remote`; X display and Firefox top-level window follow both shrink and grow dimensions | Initial: JS 1200x613, pod display 1200x612, Firefox 1200x612. Shrink: JS/display/Firefox 1000x650. Grow: JS/display/Firefox 1700x950. `localStorage.resize` stayed `remote` and KasmVNC stayed connected. | PASS | task run output | Public KasmVNC/browser surface only; no account/vault data |
| Bitwarden extension presence | Verify Bitwarden is installed without vault inspection | Source/runtime file checks under image policy and Firefox profile | Public Bitwarden XPI present and policy applied to active profile | cached XPI, distribution extension symlink, and policy path are present; no vault contents inspected | PASS | task run output | Did not open or inspect vault contents |
| Public secure-browser navigation | Verify bridge can navigate/snapshot/query/screenshot on public page | `secure_browser_navigate('https://example.com/')`; `page_snapshot`; `query`; `screenshot` | Navigation ok and sanitized outputs | Example Domain navigation, snapshot, query, and screenshot succeeded; query reported `innerWidth=1365`, `innerHeight=683`, `devicePixelRatio=1` | PASS | `/home/joy/.hermes/profiles/star/secure-browser-screenshots/secure-browser-20260702T173108Z-592953.png` | Public page only |
| Browser.eyrie KasmVNC reachability | Verify Joy-visible operator URL reaches KasmVNC | `curl -ksSL -w ... https://browser.eyrie/`; browser snapshot | 200 KasmVNC web page, no cert caveat | `curl` returned `http=200 final=https://browser.eyrie/`; page title/body contain KasmVNC assets and controls | PASS | task run output | No sensitive pages |
| Amazon safe browsing/search | Verify ordinary safe browsing and non-sensitive interactions | Prior cutover run: `secure_browser_navigate('https://www.amazon.com/')`; sanitized snapshot; type `coffee filters`; click search | Sanitized home/search; no cart/account/payment mutation | Home and search loaded; address fragments redacted; safe type/click succeeded; result snapshot sanitized | PASS | prior task run output | No add-to-cart, checkout, account, payment, or address edits |
| Amazon order-history sanitization | Rerun baseline order-history row after cutover | Requires logged-in Amazon session and ordinary secure-browser sanitized tools | Sanitized order-history proof summary if logged in; otherwise safe takeover boundary | Not rerun in the final targeted fix pass; this row still requires Joy takeover/login before rerun | NEEDS JOY TAKEOVER | none in final pass | Do not enter credentials/Bitwarden as worker |
| Owner-only post-purchase visual proof | Rerun owner-only proof binding row | `secure_browser_owner_checkout_review` only after a real order-history/post-purchase page is visible | Joy-only Telegram evidence; worker receives binding only | Not run because this requires Joy-owned sensitive account/order context | NEEDS JOY TAKEOVER | none | Avoid sensitive screenshot/login handling |
| Final purchase negative gate | Verify final purchase remains blocked | `secure_browser_guardrail_check(operation='place_order')` | Ordinary tool use denied; trusted approval required | `allowed=false`, `trusted_approval_required=true` | PASS | tool result | No checkout/final purchase page used |

## Current status

The source-managed browser.eyrie Nest Firefox image has been corrected from the interim x11vnc/noVNC shim to a source-built KasmVNC path and is live. The latest resize-grow fix is also live: the KasmVNC web client defaults to remote resizing, the wrapper keeps Firefox pinned to the live framebuffer, and targeted CDP validation shows both shrink and grow beyond the original dimensions resize the X display and Firefox window together. The live workload is ready 1/1 on the Nest Firefox image, KasmVNC autoconnects and renders public Firefox content without the noVNC client-scaling regression, the secure-browser backend identity and final-purchase guardrails still pass, and the non-sensitive parity rows have been rerun. The sensitive Amazon order-history / owner-only post-purchase proof rows still require Joy takeover/login and must not be performed by a worker.
